{
  "name": "gestao-atletas",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"php -S localhost:8000 -t public\" \"npm run dev --prefix frontend\"",
    "dev:api": "php -S localhost:8000 -t public",
    "dev:web": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
